<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.xiaolong.intents" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ColorActivity" >
        </activity>
        <activity android:name=".AlignmentActivity" >
        </activity>
        <activity android:name=".FontActivity" >
        </activity>
        <activity android:name=".TimeAndDateDisplayerActivity" >
            <intent-filter>
                <action android:name="android.intent.action.show_time" />
                <action android:name="android.intent.action.show_date" />
                <action android:name="android.intent.action.show_date_extended" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <!--
        <uses-permission
            android:name="android.permission.INTERNET">
        </uses-permission>
        -->
    </application>

</manifest>
